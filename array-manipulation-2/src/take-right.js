/* eslint-disable no-unused-vars */
const takeRight = (array, count) => {
  const result = array.slice(-count);
  return result;
};

// make storage that will contain the last count element of array;
// take off the values in the array that doesnt match the value of count;
